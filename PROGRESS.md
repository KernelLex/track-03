# Build progress

Living tracker for the autonomous build. Updated as each piece lands, not
after the fact. See `docs/LIMITATIONS.md` for the honest gaps list this
feeds into, and `eval/PREREGISTRATION.md` for what the simulation run (once
built) commits to measuring.

Status legend: ✅ built & tested · 🔶 built, not yet live-verified · ⬜ not started · ⏳ blocked on you

## Snapshot

| Area | Status | Notes |
|---|---|---|
| **Auto-orchestration** | ✅ | **The biggest gap, closed 2026-09-01.** `agent/orchestrate.py` + wiring in `agent/api/app.py` — a real webhook now triggers DIAGNOSE→DECIDE→BOUNDS→ACT automatically, no manual call. Live-verified against the real running server: real payment link created, real Telegram message sent with it, zero clicks. See docs/ORCHESTRATION.md |
| Safety/compliance engine (bounds, ledger, state machine, Auditor) | ✅ | 659 tests passing / 11 skipped (live-Razorpay-only) |
| Live Razorpay connection | ✅ | pre-existing, live-verified |
| Telegram channel | ✅ | live-verified, both send and receive, now also driven automatically by the orchestrator |
| Twilio voice channel | 🔶 | credentials confirmed on two accounts; blocked on the connected account owning a phone number (external, not a code gap) |
| Claude API extraction (Path B) | ✅ | live-verified — real extractions, correct results, spend tracked to the cent. See docs/LLM_EXTRACTION.md, docs/BUDGET.md |
| API spend tracking | ✅ | `agent/spend.py`, hard $20 ceiling enforced pre-call, real ledger at `docs/evidence/api_spend.jsonl` |
| Monte Carlo simulation harness | ✅ | `eval/personas/generator.py` + `eval/simulate.py`, `trucommit simulate`, 18 tests; not yet a pre-registered run |
| Demo dashboard (the UI) | ✅ | Published Artifact "TrueCommit Console" — live-tested end to end, reactive Telegram (real reply → real diagnosis, live) |
| Full test suite re-run | ⬜ | deferred until the 2-day plan's remaining items land, per your instruction |
| Real batch run (50+ invoices, real Razorpay calls) | ⬜ | not started — the orchestrator this depends on is now built |

## Needs from you

- **A phone number on the new Twilio account.** You switched to a different Twilio account (new Account SID + Auth Token, authenticates fine) that currently owns zero phone numbers. Claim one free in Console -> Phone Numbers -> Buy a Number, then send it and I'll wire it in. Deliberately not purchased programmatically -- that's a real account/billing action, not mine to take unilaterally.
- Still open: keep the `trucommit serve` + cloudflared tunnel running, or restart later? Both died once already this session (machine sleep or similar) and were restarted — the tunnel URL changed as a result (quick tunnels always get a fresh URL on restart). Current URL is whatever's in the dashboard's settings panel; check `tunnel.log` if it dies again.
- Per the 2-day plan: next is the real batch run (50+ synthetic invoices through actual Razorpay test-mode calls and the real webhook receiver, not an in-memory sim) and the demo video. WhatsApp integration is queued behind those, per your own priority order.

## Log

- **2026-08-31** — Started this file. Scoped the remaining build: Telegram + Twilio channels (new `agent/notify/` package, since messaging is a distinct concept from the Razorpay `Rail` protocol), the real Claude API call for Path B extraction (`agent/diagnose/llm_extract.py`), then the Monte Carlo harness. Building the credential-shaped code first so live keys can be dropped in and smoke-tested the moment they arrive, rather than waiting idle.
- **2026-08-31** — Built `agent/notify/` (protocol + `SimulatedChannel` + `TelegramChannel` + `TwilioVoiceChannel`), wired an optional `channel` param through `agent/act/executor.py` (additive — every existing caller/test is unaffected), added `"telegram"` to `ALL_CHANNELS`. Built `agent/diagnose/llm_extract.py` (the real Claude call for Path B, `claude-sonnet-5` by default, structured output via `messages.parse(output_format=ExtractionResult)`, reply text kept out of the system prompt per Law 8). Added `anthropic` as a project dependency (also had to bootstrap `pip` into the `uv`-managed `.venv`, which doesn't ship it, and confirmed the SDK's 1.x line uses `httpx2` internally, not `httpx` — matches the `claude-api` skill's own drift warning). 65 new tests, all passing, all against mocks (no real network calls in the default suite, same policy as the existing live-Razorpay opt-in tests).
  - Found and fixed a real bug this exposed: three hardcoded "all channels" test fixtures in `test_bounds_engine.py` assumed a closed 4-channel world and broke the moment a 5th channel was added — fixed to reference `ALL_CHANNELS` directly. Written up in `docs/WHAT_BROKE.md` #6.
  - Updated `docs/LIMITATIONS.md`, `docs/SETUP.md`; added `docs/CHANNELS.md` and `docs/LLM_EXTRACTION.md`; added `.env.example` and placeholder vars in `.env`; added `tools/telegram_get_chat_id.py`.
  - Full suite re-run after every change: 561 passed / 11 skipped.
  - Also installed `uv` properly (it wasn't on PATH all session — had been improvising with the venv's own `python -m pip` after bootstrapping `pip` via `ensurepip`) and regenerated `uv.lock` so the new `anthropic` dependency is actually locked, not just added to `pyproject.toml`. Discovered mid-`uv sync` that the `trucommit serve` process from earlier in the session is still running and holding a file lock on its own console-script exe — real evidence it's still alive, answering one of the two open questions without needing to ask.
- **2026-08-31** — Built the Monte Carlo simulation harness: `eval/personas/generator.py` (samples a synthetic population from the fitted Kaggle parameters — amount shape, dispute rate, the real `p_base` model — plus clearly-labelled declared-prior assumptions for what no dataset covers, e.g. contact tolerance) and `eval/simulate.py` (Arms A / B2 / C over that population; B1 skipped on purpose, matching `eval/PREREGISTRATION.md`'s own "cut B1, never B2" guidance). Arm C calls the *real* `compute_ev()` and `check_bounds()` per touch — not a stand-in — so the comparison measures the actual gate/EV logic's aggregate effect. Wired as `uv run trucommit simulate`. 18 new tests, including structural invariants (Arm C escalates to a human in cases A/B2 structurally cannot; Arm C loses fewer debtors to contact exhaustion; `EV_FLOOR` genuinely refuses when cost dominates recoverable amount — a regression test for a real finding made while building this, see `docs/SIMULATION_HARNESS.md`). Updated `eval/PREREGISTRATION.md` to reflect the harness existing without prematurely locking in the pre-registered run parameters (population size / window / primary comparison stay `PENDING` on purpose — that commitment happens in its own step, right before the first run that counts). Full suite: 579 passed / 11 skipped.
- **2026-08-31** — Real credentials arrived (Anthropic key, Twilio Account SID + API Key SID/Secret, Telegram bot token). Wrote them straight to gitignored `.env` (re-verified it's still ignored before and after), never echoed a raw value back in chat. Fixed a real gap this exposed: `TwilioVoiceChannel` only supported classic Account-SID/Auth-Token auth, but the user gave an API Key pair instead (no Auth Token) — added an `auth_username` parameter so the API Key SID can be the Basic Auth username while `account_sid` still identifies the account in the URL path (Twilio treats those as independent). Added safe, side-effect-free verification methods (`TelegramChannel.get_me()`, `TwilioVoiceChannel.verify_credentials()`) and a new `tools/verify_credentials.py` that never prints a secret. Live results: **Telegram confirmed** (bot identity), **Twilio confirmed** (account fetch via the API Key path, proving the auth fix works against the real API, not just mocks), **Anthropic blocked** — the key is identity-linked and every call 400s asking for an `anthropic-workspace-id` header. Built proper support for that (`ANTHROPIC_WORKSPACE_ID` env var, `agent/diagnose/llm_extract.py::_default_client()`) rather than guessing a value, since finding the real workspace id needs Console access this session doesn't have — asked the user instead of blocking silently. 8 new tests. Full suite: 587 passed / 11 skipped. Evidence docs (`docs/CHANNELS.md`, `docs/LLM_EXTRACTION.md`, `docs/LIMITATIONS.md`) updated with the real, dated findings.
- **2026-08-31** — User generated a workspace-scoped Anthropic key (simpler than the workspace-id header) and explicitly capped total API spend at $20, asking it be tracked. Built `agent/spend.py`: a real, append-only, git-committed spend ledger (`docs/evidence/api_spend.jsonl`) with a hard pre-call budget gate (`BudgetExceeded`, checked via a real `count_tokens()` estimate before any generating call) and known per-model pricing (Opus 5 / Sonnet 5 / Haiku 4.5). Wired into `extract_from_reply()`. First live call with the new key immediately found two more real bugs: (1) `client.messages.parse()` raises `pydantic.ValidationError` from inside its own response-parsing step when the model's JSON is schema-valid but fails ExtractionResult's own validators -- *after* the billed call happened but without returning the response, so real usage is unrecoverable in that path. Fixed with a conservative fallback record (real input tokens, worst-case output tokens, tagged `is_estimated=True`) rather than silently under-counting. (2) The actual validation failure was `promise.date="October 1st"` -- the model had no way to resolve which year without knowing today's date. Fixed by adding a second, small, uncached system block with today's date, placed after the cacheable instructions so caching still works. Re-ran live: **both extractions succeeded and were correct** (`family=C/PROMISE_STATED` and `family=D/QUANTITY_QUALITY`), total cost $0.013 of the $20 ceiling, and the real cache-read/cache-write token match across the two calls confirmed prompt caching is genuinely working, not just plausible. 8 new tests (spend math, budget gate, the estimated-fallback regression, the today's-date block). New docs: `docs/BUDGET.md`. Full suite: 606 passed / 11 skipped.
- **2026-08-31** — Built the demo dashboard the user asked for as "the main part": a published Artifact (title "TrueCommit Console") walking through B2B, subscription, and disputed-invoice-escalation scenarios, with real `check_bounds()`/`compute_ev()`/`select_instrument()` output (`tools/gen_demo_data.py`) baked in rather than hand-typed. Added a narrow backend endpoint (`agent/api/demo.py`, `/demo/trigger`) so the published page can trigger one real Telegram message or Twilio call -- deliberately hardcoded to only ever contact server-configured `DEMO_CONTACT_*` values, never a request-supplied recipient, secret-gated, rate-limited, and still runs the real `check_bounds()` gate first. CORS added to `agent/api/app.py`, scoped in effect by that endpoint's own design rather than by origin. Caught and fixed a real content bug before publishing: the escalation scenario's live trigger had no server-side text of its own and would have silently sent the wrong (b2b) message. Confirmed the whole path live via curl through the running tunnel; the dashboard's live buttons still need real `DEMO_CONTACT_TELEGRAM_CHAT_ID`/`DEMO_CONTACT_PHONE_NUMBER` values to actually fire. 13 new tests. Restarted `trucommit serve` twice to pick up the new code (same tunnel URL both times -- only the local process restarted, not `cloudflared`). Also caught and fixed a repeat of an earlier mistake: `server.err.log` got committed again from the restart (content was clean, no secrets) -- removed and added to `.gitignore` alongside `server.log`. Full suite: 617 passed / 11 skipped.
- **2026-08-31** — User messaged the Telegram bot and gave a phone number. Found the server + tunnel had both died since the last check (machine sleep or similar) -- restarted both; the tunnel's quick-URL changed as a result (expected, ephemeral by design). Set real `DEMO_CONTACT_TELEGRAM_CHAT_ID`/`DEMO_CONTACT_PHONE_NUMBER` and live-tested both dashboard buttons for real, through the actual public endpoint, not a shortcut: **Telegram send succeeded** (real message_id back). **Twilio call was cleanly refused by Twilio itself** -- a real trial-account limitation ("trial accounts have limited parameter access"), not a bug. Diagnosing it surfaced two real gaps, both fixed before reporting back: `/demo/trigger`'s response silently dropped the failure `detail`, and the dashboard's own JS checked HTTP status instead of the actual send status -- meaning this exact failure would have shown as a false "sent" in the UI. 1 new regression test. Full suite: 618 passed / 11 skipped.
- **2026-08-31** — User asked for Telegram to be "reactive" and a Twilio-free calling alternative. Recommended verifying the number in Twilio's Console first (near-zero effort) rather than building a whole new provider unprompted. Built the reactive piece for real: `TelegramChannel.get_updates()` now takes an `offset` (Telegram's own dedup mechanism); a new `/demo/check-reply` endpoint polls for a reply from the server-configured chat only (never a stranger who messages the bot mid-demo) and, when one arrives, runs it through the real `extract_from_reply()` -- a genuine, budget-tracked Claude call, not a second one for a repeat poll of the same reply (offset-based dedup, confirmed live). Dashboard now auto-polls after a live Telegram send and shows the real diagnosis inline. Caught and fixed two issues before shipping: real reply text was going into `innerHTML` unescaped (low real risk since only the demo owner's own chat can produce it, but fixed properly with an escaping helper anyway), and appending replies would have mutated the shared scenario data, duplicating across re-runs -- moved to a per-run list instead. Live-tested end to end with a real reply already sitting in the bot's queue: correctly classified with an honest low confidence (0.35) on a genuinely ambiguous one-liner. 12 new tests. Full suite: 630 passed / 11 skipped.
- **2026-08-31** — Two more Twilio credential rounds. A newly-generated API Key was rejected outright (401, Twilio code 70051, "actor doesn't have any assertions" -- no permissions granted, likely generated from a restricted-scope Console page rather than the general API keys & tokens page); reverted to the previously-working key rather than leave a broken one in place. Then the user switched to an entirely different Twilio account (new Account SID + classic Auth Token, not an API Key). That authenticates cleanly (`status: active`) -- but the account owns zero phone numbers, confirmed via a direct API check, so `TWILIO_FROM_NUMBER` is currently empty and the demo's Twilio button correctly 503s as "not configured" rather than guessing a number. Not purchasing one programmatically (the API supports it, but claiming a number is a real billing action). No test/code changes this round, docs only.
- **2026-09-01** — User handed off a full 2-day plan (deadline, hackathon scoring assessment, priority order: orchestration first, then a real batch run, then video, then WhatsApp). Adopted it. Also asked to "convert" the US-based Kaggle data flag to Indian -- checked for a real Indian dataset (Kaggle, data.gov.in) rather than relabel; none exists at the record level needed to refit `p_base`/dispute rate, so didn't fake it. Found and cited real Indian macro numbers instead (₹8.1 lakh crore in MSME delayed payments, Economic Survey 2025-26 / MSME SAMADHAAN) as honest supporting context, kept the model fit's real basis stated accurately.
  - **Built the auto-orchestrator -- the biggest gap, now closed.** `agent/orchestrate.py`: `diagnose_from_failure_code()` maps all 20 real `data/failure_taxonomy.yaml` codes to a `DiagnosisClass` (Path A, no model); `select_action_for_diagnosis()` always picks from the real `ACTIONS_UNLOCKED[family]`; `run_pipeline()` calls the real `compute_ev()`, builds a `BoundsContext` from the debtor's actual ledger history, and calls the real `execute_action()`. Explicitly not a Law 4 violation -- a driver *outside* the agents, not a ninth agent they call.
  - Wired into `agent/api/app.py`: a real `payment.failed` webhook now triggers the full pipeline automatically. Found and fixed a real bug immediately: the first version passed `disposition_for_code()` through, which selected `RETRY_CHARGE` for a RETRYABLE code -- but `RETRY_CHARGE` calls `present_debit(mandate_id, ...)` against a real mandate the webhook path doesn't have, and the very first live test crashed inside `SimulatedRail`. Fixed by never selecting `RETRY_CHARGE` without real mandate context.
  - Also closed a related gap found while live-testing: `CREATE_PAYMENT_LINK` creates a real link but doesn't itself notify anyone (it's a rail action, not a `MESSAGE_ONLY_ACTION`) -- added a real follow-up Telegram send with the actual `short_url` when a link/invoice is created and a contact is configured.
  - **Live-verified against the actual running server**, not just pytest: a real signed webhook produced a real payment link (`plink_...`) and a real Telegram message with that link, automatically, zero clicks. Hit a real debugging detour getting there -- signed webhooks kept failing verification through a manually-backgrounded server despite every env var checking out correctly; root cause turned out to be Windows `curl` reading a Git-Bash `/tmp/` path with different bytes than Python's own file read, not an env or code issue -- confirmed by switching to an inline request body.
  - 27 new tests (`tests/agent/test_orchestrate.py`, `tests/agent/test_api_orchestration.py`). New doc: `docs/ORCHESTRATION.md`. Full suite: 659 passed / 11 skipped.
