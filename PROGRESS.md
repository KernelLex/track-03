# Build progress

Living tracker for the autonomous build. Updated as each piece lands, not
after the fact. See `docs/LIMITATIONS.md` for the honest gaps list this
feeds into, and `eval/PREREGISTRATION.md` for what the simulation run (once
built) commits to measuring.

Status legend: ✅ built & tested · 🔶 built, not yet live-verified · ⬜ not started · ⏳ blocked on you

## Snapshot

| Area | Status | Notes |
|---|---|---|
| **Auto-orchestration** | ✅ | **The biggest gap, closed 2026-09-01.** `agent/orchestrate.py` + wiring in `agent/api/app.py` — a real webhook now triggers DIAGNOSE→DECIDE→BOUNDS→ACT automatically, no manual call. Live-verified against the real running server: real payment link created, real Telegram message sent with it, zero clicks. See docs/ORCHESTRATION.md |
| Safety/compliance engine (bounds, ledger, state machine, Auditor) | ✅ | 789 tests passing / 11 skipped (live-Razorpay-only) |
| **Dry-run mode / payday signal** | ✅ | `execute_action(..., dry_run=True)` — real bounds check, no rail call, never claims the idempotency key so it can't block a later real dispatch. `agent/decide/payday_signal.py` — a debtor's own past capture dates from `recovery_ledger`, grouped by day-of-month, as the free alternative to a balance check no API provides |
| **Dry-run batch (500 decisions)** | ✅ | **2026-09-01.** `tools/run_dry_run_batch.py` — 500 synthetic invoices spanning all 29 real DiagnosisClass values through the real pipeline, zero rupees moved, ledger chain verified. See docs/evidence/DRY_RUN_BATCH.md |
| Live Razorpay connection | ✅ | pre-existing, live-verified |
| Telegram channel | ✅ | live-verified, both send and receive, now also driven automatically by the orchestrator |
| **WhatsApp channel** | 🔶 | **Code-complete, tested against Meta's documented API contract (37 tests) — no real Meta account yet.** `agent/notify/whatsapp.py` + two webhook routes in `agent/api/app.py`, preferred automatically over Telegram once `WHATSAPP_PHONE_NUMBER_ID`/`WHATSAPP_ACCESS_TOKEN` are set. See docs/WHATSAPP.md |
| Twilio voice channel | 🔶 | credentials confirmed on two accounts; blocked on the connected account owning a phone number (external, not a code gap) |
| Claude API extraction (Path B) | ✅ | live-verified — real extractions, correct results, spend tracked to the cent. See docs/LLM_EXTRACTION.md, docs/BUDGET.md |
| API spend tracking | ✅ | `agent/spend.py`, hard $20 ceiling enforced pre-call, real ledger at `docs/evidence/api_spend.jsonl` |
| **Extractor-drift Auditor job** | ✅ | **2026-09-01.** `agent/auditor/extractor_drift.py` — samples logged real extractions, re-checks against a second model, quarantines below threshold. Deliberately opt-in (not auto-scheduled) since every real run spends real money |
| **Monte Carlo evaluation (§17), pre-registered and run** | ✅ | **2026-09-01, the biggest remaining spec gap, now closed.** `eval/PREREGISTRATION.md` locked (n=500, seed=42, window=30d, lift=1.0), `eval/report.py` generated `docs/RESULTS.md` from that exact config. Real findings: Arm C beats both other arms at a neutral lift with 0 real bounds violations vs hundreds for the ungated arms; `lift_prior` isn't load-bearing at realistic touch costs; a stress-tested break-even τ≈0.49 near the low end of the declared range. Also added a real violations column and a Family-B breakout (which honestly surfaced a low-power n=2 caveat) |
| **B2B early-payment discount** | ✅ | `agent/mandate/early_payment.py` — a new Family C lever, complementing the existing MSMED late-fee calc. Discount rate/window declared not fitted; behavioural lift defaults to neutral (no assumed uplift) |
| **Subscription lifecycle round-out** | ✅ | `agent/mandate/lifecycle.py` — connects the existing mandate-health detectors to real `modify_mandate`/`present_debit` calls: detect a defect, repair it, notify, wait the real 24h gate, present, capture — proven end to end against `SimulatedRail` |
| **TRAI_DND sourced** | ✅ | Real regulation numbers (Regulations 6(1) and 9, TCCCPR 2018) replace the prior `TODO` clause_ref |
| Demo dashboard (the UI) | ✅ | Published Artifact "TrueCommit Console" — live-tested end to end, reactive Telegram (real reply → real diagnosis, live). **Currently private** — needs the owner to toggle it public via the artifact's own Share menu, see "Needs from you" |
| Real scenario batch run | ✅ | 4 diverse scenarios, real Razorpay test-mode API calls — real e-mandate with a real link, real reissued invoice, real `check_bounds()` refusal. See docs/evidence/REAL_SCENARIOS.md |
| Scalability plan | ✅ | `docs/SCALABILITY.md` — a plan, not a migration. Covers the SQLite→Postgres path, multi-tenant schema, and moving orchestration off the request path |
| **Stale docs fixed** | ✅ | `docs/ARCHITECTURE.md` no longer says things like "nothing calls an LLM anywhere" or "no scheduler exists" — updated to reflect orchestration, the live Auditor scheduler, dry-run mode, and the reversal path (§11.6), which the doc never covered at all before today |
| **Persona-free headline (§26)** | ✅ | **2026-09-01.** `tools/compute_at_risk_headline.py` — ₹91,72,435 at risk on structurally defective mandates, 100% detection, zero model/persona involved in the detection. Now the README's own headline |
| **Autonomy/economics (§25)** | ✅ | Autonomy rate, cost-per-rupee-recovered, human-minutes-per-recovery added to `docs/RESULTS.md` |
| **Adversarial personas run (§24.3)** | ✅ | `tools/run_adversarial_personas.py` — 0 cases permanently stalled across 300 runs of 3 exploits against the real bounds gate. Found and fixed 2 real bugs in the harness itself along the way |
| **README rewritten to Tier-1 spec** | ✅ | Leads with the persona-free headline, then the honestly-nuanced comparative claim, updated real/simulated table, updated test count |
| Full test suite re-run | ⬜ | deferred until the 2-day plan's remaining items land, per your instruction |

## Needs from you

- **Make the dashboard public.** Both published Artifacts ("TrueCommit Console" and "Inside TrueCommit") are currently **private** — this is almost certainly why the link showed a blank screen: a private artifact renders nothing for a viewer not signed into the owning account. Open each link, use the page's own Share control, and set it to "Anyone with the link" (or however this build's UI phrases it) — this is not something I can toggle myself.
- **A phone number on the new Twilio account.** You switched to a different Twilio account (new Account SID + Auth Token, authenticates fine) that currently owns zero phone numbers. Claim one free in Console -> Phone Numbers -> Buy a Number, then send it and I'll wire it in. Deliberately not purchased programmatically -- that's a real account/billing action, not mine to take unilaterally.
- **WhatsApp: 3 real values, all from the Meta App Dashboard** — `WHATSAPP_ACCESS_TOKEN` (WhatsApp -> API Setup), `WHATSAPP_APP_SECRET` (Settings -> Basic), and a self-chosen `WHATSAPP_WEBHOOK_VERIFY_TOKEN`. The code is ready and tested; these three are the only things standing between it and going live. See docs/WHATSAPP.md.
- Still open: keep the `trucommit serve` + cloudflared tunnel running, or restart later? Both died once already this session (machine sleep or similar) and were restarted — the tunnel URL changed as a result (quick tunnels always get a fresh URL on restart). Current URL is whatever's in the dashboard's settings panel; check `tunnel.log` if it dies again.
- This Razorpay test account has now hit its test-mode payment-link cap (`test mode limit of 30 reached`) — a real, hard limit from this session's cumulative live-testing, not a bug. Further `create_payment_link` calls against this account will fail cleanly (handled, not a crash) until the account is activated/KYC'd. `create_mandate`/`create_invoice` are unaffected (separate caps, still working, live-verified again today).

## Log

- **2026-08-31** — Started this file. Scoped the remaining build: Telegram + Twilio channels (new `agent/notify/` package, since messaging is a distinct concept from the Razorpay `Rail` protocol), the real Claude API call for Path B extraction (`agent/diagnose/llm_extract.py`), then the Monte Carlo harness. Building the credential-shaped code first so live keys can be dropped in and smoke-tested the moment they arrive, rather than waiting idle.
- **2026-08-31** — Built `agent/notify/` (protocol + `SimulatedChannel` + `TelegramChannel` + `TwilioVoiceChannel`), wired an optional `channel` param through `agent/act/executor.py` (additive — every existing caller/test is unaffected), added `"telegram"` to `ALL_CHANNELS`. Built `agent/diagnose/llm_extract.py` (the real Claude call for Path B, `claude-sonnet-5` by default, structured output via `messages.parse(output_format=ExtractionResult)`, reply text kept out of the system prompt per Law 8). Added `anthropic` as a project dependency (also had to bootstrap `pip` into the `uv`-managed `.venv`, which doesn't ship it, and confirmed the SDK's 1.x line uses `httpx2` internally, not `httpx` — matches the `claude-api` skill's own drift warning). 65 new tests, all passing, all against mocks (no real network calls in the default suite, same policy as the existing live-Razorpay opt-in tests).
  - Found and fixed a real bug this exposed: three hardcoded "all channels" test fixtures in `test_bounds_engine.py` assumed a closed 4-channel world and broke the moment a 5th channel was added — fixed to reference `ALL_CHANNELS` directly. Written up in `docs/WHAT_BROKE.md` #6.
  - Updated `docs/LIMITATIONS.md`, `docs/SETUP.md`; added `docs/CHANNELS.md` and `docs/LLM_EXTRACTION.md`; added `.env.example` and placeholder vars in `.env`; added `tools/telegram_get_chat_id.py`.
  - Full suite re-run after every change: 561 passed / 11 skipped.
  - Also installed `uv` properly (it wasn't on PATH all session — had been improvising with the venv's own `python -m pip` after bootstrapping `pip` via `ensurepip`) and regenerated `uv.lock` so the new `anthropic` dependency is actually locked, not just added to `pyproject.toml`. Discovered mid-`uv sync` that the `trucommit serve` process from earlier in the session is still running and holding a file lock on its own console-script exe — real evidence it's still alive, answering one of the two open questions without needing to ask.
- **2026-08-31** — Built the Monte Carlo simulation harness: `eval/personas/generator.py` (samples a synthetic population from the fitted Kaggle parameters — amount shape, dispute rate, the real `p_base` model — plus clearly-labelled declared-prior assumptions for what no dataset covers, e.g. contact tolerance) and `eval/simulate.py` (Arms A / B2 / C over that population; B1 skipped on purpose, matching `eval/PREREGISTRATION.md`'s own "cut B1, never B2" guidance). Arm C calls the *real* `compute_ev()` and `check_bounds()` per touch — not a stand-in — so the comparison measures the actual gate/EV logic's aggregate effect. Wired as `uv run trucommit simulate`. 18 new tests, including structural invariants (Arm C escalates to a human in cases A/B2 structurally cannot; Arm C loses fewer debtors to contact exhaustion; `EV_FLOOR` genuinely refuses when cost dominates recoverable amount — a regression test for a real finding made while building this, see `docs/SIMULATION_HARNESS.md`). Updated `eval/PREREGISTRATION.md` to reflect the harness existing without prematurely locking in the pre-registered run parameters (population size / window / primary comparison stay `PENDING` on purpose — that commitment happens in its own step, right before the first run that counts). Full suite: 579 passed / 11 skipped.
- **2026-08-31** — Real credentials arrived (Anthropic key, Twilio Account SID + API Key SID/Secret, Telegram bot token). Wrote them straight to gitignored `.env` (re-verified it's still ignored before and after), never echoed a raw value back in chat. Fixed a real gap this exposed: `TwilioVoiceChannel` only supported classic Account-SID/Auth-Token auth, but the user gave an API Key pair instead (no Auth Token) — added an `auth_username` parameter so the API Key SID can be the Basic Auth username while `account_sid` still identifies the account in the URL path (Twilio treats those as independent). Added safe, side-effect-free verification methods (`TelegramChannel.get_me()`, `TwilioVoiceChannel.verify_credentials()`) and a new `tools/verify_credentials.py` that never prints a secret. Live results: **Telegram confirmed** (bot identity), **Twilio confirmed** (account fetch via the API Key path, proving the auth fix works against the real API, not just mocks), **Anthropic blocked** — the key is identity-linked and every call 400s asking for an `anthropic-workspace-id` header. Built proper support for that (`ANTHROPIC_WORKSPACE_ID` env var, `agent/diagnose/llm_extract.py::_default_client()`) rather than guessing a value, since finding the real workspace id needs Console access this session doesn't have — asked the user instead of blocking silently. 8 new tests. Full suite: 587 passed / 11 skipped. Evidence docs (`docs/CHANNELS.md`, `docs/LLM_EXTRACTION.md`, `docs/LIMITATIONS.md`) updated with the real, dated findings.
- **2026-08-31** — User generated a workspace-scoped Anthropic key (simpler than the workspace-id header) and explicitly capped total API spend at $20, asking it be tracked. Built `agent/spend.py`: a real, append-only, git-committed spend ledger (`docs/evidence/api_spend.jsonl`) with a hard pre-call budget gate (`BudgetExceeded`, checked via a real `count_tokens()` estimate before any generating call) and known per-model pricing (Opus 5 / Sonnet 5 / Haiku 4.5). Wired into `extract_from_reply()`. First live call with the new key immediately found two more real bugs: (1) `client.messages.parse()` raises `pydantic.ValidationError` from inside its own response-parsing step when the model's JSON is schema-valid but fails ExtractionResult's own validators -- *after* the billed call happened but without returning the response, so real usage is unrecoverable in that path. Fixed with a conservative fallback record (real input tokens, worst-case output tokens, tagged `is_estimated=True`) rather than silently under-counting. (2) The actual validation failure was `promise.date="October 1st"` -- the model had no way to resolve which year without knowing today's date. Fixed by adding a second, small, uncached system block with today's date, placed after the cacheable instructions so caching still works. Re-ran live: **both extractions succeeded and were correct** (`family=C/PROMISE_STATED` and `family=D/QUANTITY_QUALITY`), total cost $0.013 of the $20 ceiling, and the real cache-read/cache-write token match across the two calls confirmed prompt caching is genuinely working, not just plausible. 8 new tests (spend math, budget gate, the estimated-fallback regression, the today's-date block). New docs: `docs/BUDGET.md`. Full suite: 606 passed / 11 skipped.
- **2026-08-31** — Built the demo dashboard the user asked for as "the main part": a published Artifact (title "TrueCommit Console") walking through B2B, subscription, and disputed-invoice-escalation scenarios, with real `check_bounds()`/`compute_ev()`/`select_instrument()` output (`tools/gen_demo_data.py`) baked in rather than hand-typed. Added a narrow backend endpoint (`agent/api/demo.py`, `/demo/trigger`) so the published page can trigger one real Telegram message or Twilio call -- deliberately hardcoded to only ever contact server-configured `DEMO_CONTACT_*` values, never a request-supplied recipient, secret-gated, rate-limited, and still runs the real `check_bounds()` gate first. CORS added to `agent/api/app.py`, scoped in effect by that endpoint's own design rather than by origin. Caught and fixed a real content bug before publishing: the escalation scenario's live trigger had no server-side text of its own and would have silently sent the wrong (b2b) message. Confirmed the whole path live via curl through the running tunnel; the dashboard's live buttons still need real `DEMO_CONTACT_TELEGRAM_CHAT_ID`/`DEMO_CONTACT_PHONE_NUMBER` values to actually fire. 13 new tests. Restarted `trucommit serve` twice to pick up the new code (same tunnel URL both times -- only the local process restarted, not `cloudflared`). Also caught and fixed a repeat of an earlier mistake: `server.err.log` got committed again from the restart (content was clean, no secrets) -- removed and added to `.gitignore` alongside `server.log`. Full suite: 617 passed / 11 skipped.
- **2026-08-31** — User messaged the Telegram bot and gave a phone number. Found the server + tunnel had both died since the last check (machine sleep or similar) -- restarted both; the tunnel's quick-URL changed as a result (expected, ephemeral by design). Set real `DEMO_CONTACT_TELEGRAM_CHAT_ID`/`DEMO_CONTACT_PHONE_NUMBER` and live-tested both dashboard buttons for real, through the actual public endpoint, not a shortcut: **Telegram send succeeded** (real message_id back). **Twilio call was cleanly refused by Twilio itself** -- a real trial-account limitation ("trial accounts have limited parameter access"), not a bug. Diagnosing it surfaced two real gaps, both fixed before reporting back: `/demo/trigger`'s response silently dropped the failure `detail`, and the dashboard's own JS checked HTTP status instead of the actual send status -- meaning this exact failure would have shown as a false "sent" in the UI. 1 new regression test. Full suite: 618 passed / 11 skipped.
- **2026-08-31** — User asked for Telegram to be "reactive" and a Twilio-free calling alternative. Recommended verifying the number in Twilio's Console first (near-zero effort) rather than building a whole new provider unprompted. Built the reactive piece for real: `TelegramChannel.get_updates()` now takes an `offset` (Telegram's own dedup mechanism); a new `/demo/check-reply` endpoint polls for a reply from the server-configured chat only (never a stranger who messages the bot mid-demo) and, when one arrives, runs it through the real `extract_from_reply()` -- a genuine, budget-tracked Claude call, not a second one for a repeat poll of the same reply (offset-based dedup, confirmed live). Dashboard now auto-polls after a live Telegram send and shows the real diagnosis inline. Caught and fixed two issues before shipping: real reply text was going into `innerHTML` unescaped (low real risk since only the demo owner's own chat can produce it, but fixed properly with an escaping helper anyway), and appending replies would have mutated the shared scenario data, duplicating across re-runs -- moved to a per-run list instead. Live-tested end to end with a real reply already sitting in the bot's queue: correctly classified with an honest low confidence (0.35) on a genuinely ambiguous one-liner. 12 new tests. Full suite: 630 passed / 11 skipped.
- **2026-08-31** — Two more Twilio credential rounds. A newly-generated API Key was rejected outright (401, Twilio code 70051, "actor doesn't have any assertions" -- no permissions granted, likely generated from a restricted-scope Console page rather than the general API keys & tokens page); reverted to the previously-working key rather than leave a broken one in place. Then the user switched to an entirely different Twilio account (new Account SID + classic Auth Token, not an API Key). That authenticates cleanly (`status: active`) -- but the account owns zero phone numbers, confirmed via a direct API check, so `TWILIO_FROM_NUMBER` is currently empty and the demo's Twilio button correctly 503s as "not configured" rather than guessing a number. Not purchasing one programmatically (the API supports it, but claiming a number is a real billing action). No test/code changes this round, docs only.
- **2026-09-01** — User handed off a full 2-day plan (deadline, hackathon scoring assessment, priority order: orchestration first, then a real batch run, then video, then WhatsApp). Adopted it. Also asked to "convert" the US-based Kaggle data flag to Indian -- checked for a real Indian dataset (Kaggle, data.gov.in) rather than relabel; none exists at the record level needed to refit `p_base`/dispute rate, so didn't fake it. Found and cited real Indian macro numbers instead (₹8.1 lakh crore in MSME delayed payments, Economic Survey 2025-26 / MSME SAMADHAAN) as honest supporting context, kept the model fit's real basis stated accurately.
  - **Built the auto-orchestrator -- the biggest gap, now closed.** `agent/orchestrate.py`: `diagnose_from_failure_code()` maps all 20 real `data/failure_taxonomy.yaml` codes to a `DiagnosisClass` (Path A, no model); `select_action_for_diagnosis()` always picks from the real `ACTIONS_UNLOCKED[family]`; `run_pipeline()` calls the real `compute_ev()`, builds a `BoundsContext` from the debtor's actual ledger history, and calls the real `execute_action()`. Explicitly not a Law 4 violation -- a driver *outside* the agents, not a ninth agent they call.
  - Wired into `agent/api/app.py`: a real `payment.failed` webhook now triggers the full pipeline automatically. Found and fixed a real bug immediately: the first version passed `disposition_for_code()` through, which selected `RETRY_CHARGE` for a RETRYABLE code -- but `RETRY_CHARGE` calls `present_debit(mandate_id, ...)` against a real mandate the webhook path doesn't have, and the very first live test crashed inside `SimulatedRail`. Fixed by never selecting `RETRY_CHARGE` without real mandate context.
  - Also closed a related gap found while live-testing: `CREATE_PAYMENT_LINK` creates a real link but doesn't itself notify anyone (it's a rail action, not a `MESSAGE_ONLY_ACTION`) -- added a real follow-up Telegram send with the actual `short_url` when a link/invoice is created and a contact is configured.
  - **Live-verified against the actual running server**, not just pytest: a real signed webhook produced a real payment link (`plink_...`) and a real Telegram message with that link, automatically, zero clicks. Hit a real debugging detour getting there -- signed webhooks kept failing verification through a manually-backgrounded server despite every env var checking out correctly; root cause turned out to be Windows `curl` reading a Git-Bash `/tmp/` path with different bytes than Python's own file read, not an env or code issue -- confirmed by switching to an inline request body.
  - 27 new tests (`tests/agent/test_orchestrate.py`, `tests/agent/test_api_orchestration.py`). New doc: `docs/ORCHESTRATION.md`. Full suite: 659 passed / 11 skipped.
- **2026-09-01** — User asked to finish everything possible ahead of the Twilio phone number and WhatsApp credentials arriving, get 4 real test scenarios with e-mandate links, make the dashboard public and scalable, and reported one published link rendering as a blank screen.
  - **Diagnosed the blank screen**: read both published Artifacts back and confirmed they're live and serving correct content — the actual issue is that both are marked **private**. A private artifact renders blank for a viewer not signed into the owning account, which fits the symptom exactly. Not a code bug; needs the owner to use the artifact's own Share control (see "Needs from you").
  - **Built the WhatsApp channel** (`agent/notify/whatsapp.py`): Meta Graph API v25.0 directly, not Twilio's reseller layer (per the 2026-09-01 handoff doc's explicit instruction). `send()` (free-form, 24h-window), `send_template()` (dynamic URL button restricted to a suffix, matching Meta's real constraint), `send_interactive_buttons()`, `verify_webhook_signature()` (X-Hub-Signature-256), `verify_webhook_challenge()` (Meta's GET handshake), `parse_incoming_messages()` (text vs. structured button-reply). Wired two new routes into `agent/api/app.py` (`GET`/`POST /webhooks/whatsapp`) — caught and fixed a real routing bug before it shipped: they were initially registered *after* the generic `/webhooks/{source}` route, which Starlette would have matched first, silently swallowing every WhatsApp webhook into the Razorpay handler. `lifespan()` now prefers `WhatsAppChannel` over `TelegramChannel` automatically once both `WHATSAPP_PHONE_NUMBER_ID`/`WHATSAPP_ACCESS_TOKEN` are set. Inbound messages are diagnosed immediately (Path A for a button tap, real Path B `extract_from_reply()` for free text) and deduplicated against Meta's own redelivery using the same `EventStore` every Razorpay webhook already dedupes through — without that, a retried delivery would double-bill a real Claude call. 37 new tests (`test_whatsapp_channel.py`, `test_whatsapp_webhook_routes.py`), all against `httpx.MockTransport`/Meta's documented shapes — no real Meta account exists yet. New doc: `docs/WHATSAPP.md`.
  - **Built the real scenario batch run** (`tools/run_real_scenarios.py`): 4 deliberately distinct scenarios (instrument failure → real payment link, liquidity → real e-mandate setup, administrative defect → real reissued invoice, dispute → a deliberately wrong action caught by `check_bounds()`), each through the real fitted `p_base` model, real `compute_ev()`, a real `BoundsContext` built from actual ledger history, and real `execute_action()` calls against `RazorpayRail` — not `SimulatedRail`. Found and fixed a real gap while building this: `Mandate` (the e-mandate type) never captured the real customer-facing `short_url` Razorpay's Subscription API actually returns, even though `PaymentLink`/`Invoice` both already did — added it to the type and to both rails' `create_mandate()`, plus the executor's outcome detail, so the ledger now genuinely records the real e-mandate link. Live results: a real Subscription created (`sub_TWXBIauYDEWf5j`, real link `https://rzp.io/rzp/QZQetflq`) and a real Invoice (`inv_TWXBK2f7v7imND`) — both independently checkable in the Razorpay test dashboard — plus a real `NO_MANDATE_ON_DISPUTE` refusal proving the safety gate actually blocks a bad real action, not just a hypothetical one. The 4th scenario (payment link) hit a genuine infra limit — this test account's test-mode payment-link cap, already exhausted by this session's cumulative live-testing — caught cleanly and recorded as evidence rather than crashing the batch, which is itself a useful demonstration (`RazorpayRail` doesn't currently wrap this into `RailUnavailable`, documented honestly as a real gap, not fixed in this pass). Ledger chain verified intact. Evidence: `docs/evidence/REAL_SCENARIOS.md`, `docs/evidence/real_scenarios_*.json`, `docs/evidence/real_scenarios_ledger.db`.
  - **Wrote `docs/SCALABILITY.md`** — a plan, not a migration, per the standing instruction to keep scaling "in the picture" without building it now: the SQLite-per-file → Postgres path (schema is already near-portable, no SQLite-specific syntax in play), the multi-tenant `merchant_id` scoping gap this build genuinely has today, moving orchestration off the request path onto a queue, and making the API layer stateless/horizontally scalable. Explicit about what does *not* need to change (the ledger's hash-chain design, the bounds engine, the channel/rail abstractions — all already scale-agnostic).
  - 37 new tests this session (WhatsApp), full suite: 696 passed / 11 skipped.
- **2026-09-01** — User asked whether NPCI's per-mandate attempt cap could be made unlimited (no — it's an NPCI-level rail constraint, not a Razorpay setting or a code gap, and it only affects the mandate/subscription side, not B2B payment links, which have no attempt limit) and whether "test in test-mode, then fire for real" would predict a real debit clearing (no — test mode validates plumbing, not a real bank balance, since no API anywhere exposes balance data to a merchant and a probe debit would just burn a real attempt slot). Implemented the two concrete things that came out of that discussion:
  - **Dry-run mode on ACT** (`agent/act/executor.py`, `agent/orchestrate.py`): `execute_action(..., dry_run=True)` runs `check_bounds()` for real (a refusal is still a refusal) but never claims the outbound idempotency key and never calls the rail or a channel — logs what it *would* have dispatched instead. Deliberately never claims the idempotency key, so a dry run can never collide with, or block, a later real dispatch of the identical action — proven by a test that dry-runs an action three times then dispatches it for real right after. `run_pipeline()` passes `dry_run` straight through, so DECIDE and BOUNDS stay genuine while ACT goes silent. This is Law 9's reversibility principle applied *before* the fact: a batch of invoices can now be pushed through the full real pipeline with zero rupees able to move.
  - **Payday signal** (`agent/decide/payday_signal.py`): a free, already-owned alternative to a live balance check, since none exists. Every past `captured` payment is already timestamped in `recovery_ledger` (Law 7); `compute_payday_signal()` groups a debtor's own past capture dates by day-of-month and reports which days recur, with an honest zero-confidence result below 2 samples or with no recurring day — not a guess dressed up as a signal. Added `RecoveryLedger.entries_for_debtor()` (was missing — only `entries_for_invoice` existed) and an optional `recorded_at` override on `attribute()` for backfill/testing.
  - 19 new tests (`test_payday_signal.py`, `TestDryRun` in `test_act_executor.py`, `TestRunPipelineDryRun` in `test_orchestrate.py`). Full suite: 715 passed / 11 skipped.
- **2026-09-01** — User asked which of the two "not built" feature ideas from earlier (subscription debit-timing via SMS/Account Aggregator; B2B early-payment discount) should actually be built, then asked for a full status read and a plan for everything doable before the Twilio/WhatsApp keys arrive. Gave both straight: build the B2B discount (tractable, no new credentials); leave Account Aggregator as roadmap only (too large a lift, already correctly scoped that way). Proposed and then executed a six-item plan, in full:
  - **Sourced `TRAI_DND`**, replacing its `TODO` clause_ref with real regulation numbers (Regulations 6(1) and 9, TCCCPR 2018), fetched via a secondary legal database and flagged at the same weaker sourcing tier as `RBI_FPC_HOURS` — a real citation now, not independently verified against TRAI's own primary PDF.
  - **Built `agent/mandate/early_payment.py`** — a genuinely new Family C lever (a voluntary early-payment discount, the B2B complement to the existing MSMED late-fee calc). Discount rate/window are declared, not fitted; the behavioural lift defaults to `Prior(1.0)` — no assumed uplift, so a discount is honestly EV-negative unless a caller supplies real evidence otherwise, the same swept-parameter discipline `lift_prior` itself follows.
  - **Built `agent/mandate/lifecycle.py`**, connecting the pre-existing mandate-health detectors to real `modify_mandate`/`present_debit` rail calls for the first time: detect a defect, repair it, send the mandatory notice, wait the real 24h gate, present the debit, capture — proven end to end against `SimulatedRail` (the one rail where both calls actually work; `RazorpayRail` still honestly raises `RailUnavailable`). Found and fixed a real gap while building this: `SimulatedRail.fetch()` had no `"mandates"` kind at all.
  - **Built the extractor-drift Auditor job** (`agent/auditor/extraction_log.py` + `agent/auditor/extractor_drift.py`) — DEVDOC_v6's last "not implemented" Auditor job, unblocked now that Path B is live. Samples logged real extractions, re-checks against a second model (Haiku 4.5), quarantines below an agreement threshold. Wired into the one place a real extraction already happens (`demo.py`'s reactive-reply endpoint) so real usage populates real data. Found and fixed a real bug immediately: the log's default path is relative, so calling it without an env override wrote a real `extraction_log.db` into the repo root during test runs — fixed with a `TRUECOMMIT_EXTRACTION_LOG` env var. Deliberately **not** auto-scheduled like the two free Auditor jobs — every real drift check spends real money against the $20 ceiling, and that's a decision left to whoever runs the server, not made silently.
  - **Ran the pre-registered §17 evaluation — the biggest remaining spec gap, now closed.** Locked `eval/PREREGISTRATION.md` (n=500, seed=42, window=30d, primary comparison at a *neutral* `lift_prior=1.0`, chosen specifically because it needs no assumed behavioural uplift to be interesting) as its own commit before running anything, then built `eval/report.py` to generate `docs/RESULTS.md` from exactly that configuration. Extended the harness with two things §17.7 asks for that it didn't have: a real `check_bounds()` "violations column" for the ungated arms (a shadow check, never gating them, narrowly scoped to a real, triggerable `DISPUTE_FREEZE` case) and a Family-B-only breakout. Real, sometimes complicating findings, all reported honestly: Arm C beats both other arms on recovery *and* commits zero violations against hundreds for the ungated arms; at the realistic Rs 5 touch cost `lift_prior` turns out not to be load-bearing at all (`EV_FLOOR` never binds against this population); a stress-tested elevated touch cost does produce a genuine break-even τ≈0.49; the Family-B breakout landed only 2 of 500 personas in that subpopulation (a direct, honest consequence of the fitted `p_base` model's own high base rate) — too few to be a reliable estimate, reported as such rather than hidden. Found and fixed a real bug while building the break-even search: a naive "first point where C >= A" search reported the *lowest tested lift* as "the break-even" even when C dominates at every point and doesn't depend on the parameter at all — fixed with a three-way `BreakEven` classification (`dominates_throughout` / `crosses` / `never_catches_up`) instead of collapsing to a single value-or-None.
  - **Built a dry-run batch tool** (`tools/run_dry_run_batch.py`) reusing `dry_run=True` from earlier this session: 500 synthetic invoices spanning all 29 real `DiagnosisClass` values across all four families, pushed through the real `run_pipeline()`, zero rupees able to move (asserted, not just claimed — the script checks zero rail objects were created before writing any evidence out). Reported an honest 0% bounds-refusal rate rather than manufacturing one for effect, with a note explaining why (unique first-time debtors, no disputed-amount parameter on this path) and a pointer to `tools/run_real_scenarios.py` for where a real refusal actually lives.
  - **Fixed the stale `docs/ARCHITECTURE.md`**, which still said things like "nothing calls an LLM anywhere in this codebase" and "there is still no scheduler and no orchestrating process" — both false since the orchestration/LLM/scheduler work landed. Corrected every stale `[pending]` marker this pass touched, and added a "Reversal (§11.6)" section the doc had never covered at all despite `agent/act/reversal.py` existing.
  - 56 new tests across this whole batch (early-payment: 17, mandate lifecycle: 24 including one full-lifecycle end-to-end test, extraction log: 4, extractor drift: 11, auditor scheduler additions: 5, eval harness violations/Family-B/raw: 13, break-even classification: 4). Full suite: 771 passed / 11 skipped.
- **2026-09-01** — User asked for the remaining status gaps to be closed, minus anything needing human input (the live-rail ceiling and the Twilio/WhatsApp credentials, both out of scope for more code). Built the three remaining spec sections:
  - **§26's persona-free headline** (`tools/compute_at_risk_headline.py`): ₹91,72,435 at risk across a batch of synthetic mandates deliberately constructed with real object-shape defects (undersized headroom, an expiry preceding the next debit) — `check_mandate_health()` catches all of them, 100% detection, 0 false positives/negatives, since detection here is deterministic arithmetic, not a persona prediction. Honest about the one thing that *is* a modelling choice: the defect rate in the synthetic batch is a declared construction parameter, not a measured real-world prevalence, stated plainly rather than left implicit.
  - **§25's autonomy rate and unit economics**, added to `docs/RESULTS.md` (`eval/report.py`): cost-per-rupee-recovered and human-minutes-per-recovery, the latter resting on one new, explicitly named assumption (`ASSUMED_MINUTES_PER_ESCALATION`) rather than smuggled in as measured. Arm C's lower autonomy rate (90.4% vs the two ungated arms' 100%) is explained as the gate correctly working, not a weakness — the ungated arms' "100%" is the absence of a safety net, not a capability.
  - **§24.3's adversarial personas, run for real** (`eval/personas/adversarial/strategies.py`, `tools/run_adversarial_personas.py`): the three mechanically-distinct exploits (SERIAL_PROMISER, DISPUTE_ABUSER, CHANNEL_HOPPER) driven through the real `check_bounds()` gate over a synthetic population — 0 cases permanently stalled across 300 runs. Found and fixed two real bugs in the test harness itself while building this (a missing `rail_tag` that made every attempt fail `RAIL_DISCLOSURE`; `escalate_human` incorrectly tagged with the debtor's own already-opted-out channel, which let the unrelated `TRAI_DND` rule block escalation too) — caught and fixed before any evidence was generated, not after. `INJECTOR` deliberately not simulated here: this synthetic harness has no live model or free-text path to exercise that exploit against, and `tests/agent/test_injection_resistance.py` (80 tests) already proves it properly.
  - **Rewrote `README.md`** to DEVDOC_v6 §20's actual Tier-1 spec — it previously still said "This README does not yet have the headline numbers... because the eval harness... doesn't exist yet" and cited "~490 tests," both false since the eval harness ran and the suite is well past that. Now leads with the persona-free headline, then the honestly-nuanced comparative claim (not forced into a single number — the real story is that `lift_prior` isn't load-bearing at realistic cost, and only becomes so under an explicit stress test), an updated real/simulated table, and a pointer to exactly what's still blocked on the user rather than on more code.
  - 22 new tests this pass (economics: 11, adversarial strategies: 11). Full suite: 789 passed / 11 skipped.
