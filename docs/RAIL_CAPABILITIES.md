# Rail Capabilities — PENDING

This file is supposed to be generated by `tools/probe_rails.py` (DEVDOC_v6
§6), run against a real Razorpay test-mode account
(`RAZORPAY_KEY_ID` / `RAZORPAY_KEY_SECRET` env vars — free at signup, no KYC
needed, §5.1). **It has not been run yet** — no test keys were available
while building this project — so nothing below is real capability data.

Do not treat the "Expected" table in DEVDOC_v6 §6 as a substitute for
actually running the probe. Run it and let this file be overwritten:

```
RAZORPAY_KEY_ID=rzp_test_xxx RAZORPAY_KEY_SECRET=xxx uv run python tools/probe_rails.py
```

`tools/probe_rails.py` itself has never executed against a live account —
it was written against the installed `razorpay-python` SDK's verified
interface (see `docs/SIMULATOR_PROVENANCE.md` §2 for exactly what was
checked and how), but its actual HTTP behaviour against Razorpay's servers
is unconfirmed. Treat the first real run as the actual measurement, and
read its output — including for probes that error in a way this script
didn't anticipate — before trusting any capability claim elsewhere in this
project's docs.
