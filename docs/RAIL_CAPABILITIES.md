# Rail Capabilities

Generated by `tools/probe_rails.py` at 2026-08-30T17:48:26+00:00. Re-run before submission — see DEVDOC_v6 §6.

| Probe | Reachable | HTTP status | Error code | Description |
|---|---|---|---|---|
| `orders` | cleared | 200 | — | reachable |
| `payment_links` | cleared | 200 | — | reachable |
| `invoices` | cleared | 200 | — | reachable |
| `customers` | cleared | 200 | — | reachable |
| `plans` | cleared | 200 | — | reachable |
| `subscriptions` | cleared | 200 | — | reachable |
| `tokens_recurring` | blocked | — | NOT_DIRECTLY_PROBEABLE | Recurring card tokenization happens through a checkout flow (recurring=1 on an order), not a standalone server-side create call — this probe cannot exercise it without a real checkout session. Verify  |
| `upi_autopay` | blocked | — | NOT_DIRECTLY_PROBEABLE | UPI Autopay S2S needs explicit approval and a specific subscription auth_type — the subscriptions probe above will surface the enablement error if the account lacks it, but a clean subscriptions succe |
| `emandate` | blocked | — | NOT_DIRECTLY_PROBEABLE | eNACH/eMandate enablement is bank-dependent and dashboard-configured (§5.1) — no documented standalone API call confirms it in isolation. Verify manually via the dashboard's payment methods page. |
| `settlements` | cleared | 200 | — | reachable |

`cleared` means the probe's call succeeded — it does not by itself prove every operation on that object works, only that creation did (§5.4's conformance scope note applies here too).

## Mandates in practice (live-verified 2026-09-01)

The `emandate` probe above is `NOT_DIRECTLY_PROBEABLE` and stays that way —
eNACH enablement is bank-dependent and dashboard-configured, and no
standalone API call confirms it. What *is* confirmed is the primitive this
project actually builds mandates on: **Plan + Subscription**, both `cleared`
above and both exercised live.

A real create returned:

```
plan:   plan_TWqmzuFOtBMvBr
sub:    sub_TWqn06B4fhFLLE   status=created
short_url: https://rzp.io/rzp/F1Ied9y
```

Three properties this pins down, each of which shaped the code:

- **`short_url` is real and present.** A subscription in `created` state
  hands back a customer-facing authorization page. That is what makes an
  "e-mandate link" a real deliverable rather than a description of one.
- **`start_at` is accepted.** A mandate can be scheduled for the date the
  debtor named rather than debiting on authorization.
  `RazorpayRail.create_mandate()` now passes it through when it parses to a
  future instant, and drops it otherwise — Razorpay refuses a backdated
  `start_at`, and a mandate the debtor can still authorize beats a hard
  failure over a stale date.
- **The per-cycle amount is fixed.** A plan carries one amount, so one
  subscription cannot express a split with unequal legs. This is the
  constraint behind `agent/mandate/emandate.py` issuing one mandate per
  distinct instalment amount — see `docs/DEMO_UI.md`, "The e-mandate is
  real, and it is the point", for why that resolution was chosen over
  rounding the legs or authorizing at the larger amount.

`status=created` is an authorization *request*. No money moves until the
debtor completes authentication themselves, which is what makes it safe to
send the link alongside a proposal.

### The auth method cannot be pinned from the API (probed 2026-09-01)

Razorpay's docs describe an `auth_type` on subscription create (netbanking,
debitcard, aadhaar, ...). On this account it is rejected outright. Every
value tried returned the same `BadRequestError`:

```
[netbanking ] FAIL  auth_type is/are not required and should not be sent
[debitcard  ] FAIL  auth_type is/are not required and should not be sent
[aadhaar    ] FAIL  auth_type is/are not required and should not be sent
[emandate   ] FAIL  auth_type is/are not required and should not be sent
[nach       ] FAIL  auth_type is/are not required and should not be sent
[upi        ] FAIL  auth_type is/are not required and should not be sent
[omitted    ] OK    sub_TWrAHtjw67klmf  status=created  short_url=https://rzp.io/rzp/FBDmpE1p
```

Only the request with no `auth_type` at all succeeds. The debtor chooses
their authentication method on the hosted page the `short_url` opens.

This is the reason `agent/mandate/rail_capability.py` describes its output
as a *netbanking/eNACH e-mandate* -- the instrument class this account can
issue -- rather than claiming the code selects the netbanking channel. It
does not, and this probe is why that distinction is drawn explicitly
rather than glossed.

The same finding is what makes UPI Autopay's `blocked` row above concrete:
its enablement needs "a specific subscription auth_type" that this account
cannot send at all.
